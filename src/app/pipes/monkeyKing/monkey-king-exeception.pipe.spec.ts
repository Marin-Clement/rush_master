import { MonkeyKingExeceptionPipe } from './monkey-king-exeception.pipe';

describe('MonkeyKingExeceptionPipe', () => {
  it('create an instance', () => {
    const pipe = new MonkeyKingExeceptionPipe();
    expect(pipe).toBeTruthy();
  });
});
